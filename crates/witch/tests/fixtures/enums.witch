enum Lorem {
    Ipsum,
    Dolor
}

enum Foobar {
    Foo,
    Bar,
    Baz(Lorem)
}

enum Option {
    None,
    Some
}

let x = Some

function is_some(maybe: Option) -> string {
    return case maybe {
        Some -> { true } 
        None -> { false }
    }
}

is_some(x)